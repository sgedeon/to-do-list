export let object = class {
  constructor (nom, importance, date, id, fait) {
      this.nom = nom;
      this.importance = importance;
      this.date = date;
      this.id = id;
      this.fait = fait;
  }
}

